export interface CostCardInterface {
    name: string,
    description: string,
    price: string,
    priceDesc: string,
    discount: string,
    discountedPrice: string,
    about: string[],
    unique: boolean,
}